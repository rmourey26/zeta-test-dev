import type { ZetaChainClient } from "./client";
export declare const trackCCTX: (this: ZetaChainClient, { hash, json, emitter, }: {
    emitter: any;
    hash: string;
    json: Boolean;
}) => Promise<void>;
