import { ZetaChainClient } from "./client";
export interface TokenBalance {
    balance: string;
    chain_id: number | string;
    chain_name: string;
    coin_type: string;
    contract?: string;
    decimals: number;
    id: string;
    symbol: string;
    ticker: string;
    zrc20?: string;
}
/**
 * Get token balances of all tokens on all chains connected to ZetaChain.
 *
 * @param this - ZetaChainClient instance.
 * @param options.evmAddress EVM address
 * @param options.btcAddress Bitcoin address
 * @returns
 */
export declare const getBalances: (this: ZetaChainClient, { evmAddress, btcAddress }: {
    btcAddress?: string;
    evmAddress: string;
}) => Promise<TokenBalance[]>;
