import { ZetaChainClient } from "./client";
type Fees = {
    messaging: Array<{
        [key: string]: string;
    }>;
    omnichain: Array<{
        [key: string]: string;
    }>;
};
export declare const getFees: (this: ZetaChainClient, gas: Number) => Promise<Fees>;
export {};
